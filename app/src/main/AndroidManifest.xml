<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vibgreon.emicalculator" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMICALCULATOR" >
        <activity android:name=".NewActivity113" >
        </activity>
        <activity android:name=".NewActivity112" />
        <activity android:name=".NewActivity111" />
        <activity android:name=".NewActivity11" />
        <activity android:name=".NewActivity2" />
        <activity android:name=".NewActivity1" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>